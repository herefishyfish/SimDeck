# SimDeck Release Workflow
#
# Triggered manually from the Actions tab. Picks one package, bumps its version
# locally with `npm version` or reuses the current version, commits + tags,
# builds, and publishes:
#   - npm packages publish over OIDC trusted publishing (no NPM_TOKEN).
#   - The VS Code extension publishes via vsce + VSCE_PAT (no OIDC option).
#
# Jobs:
#   prepare        Resolves package metadata, bumps the version, commits + tags,
#                  and pushes. Runs first so every native binary is built from
#                  the bumped commit (the CLI bakes in CARGO_PKG_VERSION).
#   build-windows  Builds build/simdeck-bin-win32-x64.exe on windows-latest and
#                  uploads it. Skipped for every package except the root CLI.
#   release        Builds the macOS binaries, signs + notarizes them, pulls in
#                  the Windows artifact, and publishes.
#
# OIDC setup (do once per npm package on npmjs.com):
#   Package -> Settings -> Trusted Publishers -> GitHub Actions
#     Repository:     <owner>/SimDeck
#     Workflow:       release.yml
#     Environment:    npm-publish
#
# Trusted publishers must be configured for each of:
#   simdeck
#   react-native-simdeck
#   @nativescript/simdeck-inspector
#
# Repository secrets / vars:
#   VSCE_PAT      (only required when publishing simdeck-vscode)
#   APPLE_TEAM_ID (required when publishing simdeck)
#
# Tag scheme:
#   <slug>-v<version>        e.g. simdeck-v0.2.0
#                                  react-native-simdeck-v0.2.0
#                                  nativescript-simdeck-inspector-v0.2.0
#                                  simdeck-vscode-v0.2.0

name: Release

on:
  workflow_dispatch:
    inputs:
      package:
        description: "Package to release"
        required: true
        type: choice
        options:
          - simdeck
          - react-native-simdeck
          - "@nativescript/simdeck-inspector"
          - simdeck-vscode
      bump:
        description: "Version bump (use current to publish the checked-in version without bumping)"
        required: true
        type: choice
        default: prerelease
        options:
          - current
          - prerelease
          - patch
          - minor
          - major
      preid:
        description: "Prerelease identifier (only used when bump=prerelease)"
        required: false
        type: string
        default: next
      dist-tag:
        description: "npm dist-tag (default: prerelease->preid, otherwise 'latest')"
        required: false
        type: string
        default: ""
      dry-run:
        description: "Skip git push and publish; print what would happen"
        required: false
        type: boolean
        default: false

concurrency:
  group: simdeck-release-${{ inputs.package }}
  cancel-in-progress: false

permissions:
  contents: write # commit + tag + GitHub release
  id-token: write # OIDC trusted publishing + npm provenance

jobs:
  # The version bump has to happen before any binary is built: the CLI declares
  # `#[command(version)]`, so the version is baked into every native binary from
  # Cargo.toml. Bumping here and having the build jobs check out the resulting
  # commit keeps the macOS and Windows binaries reporting the same version.
  prepare:
    name: Prepare ${{ inputs.package }} (${{ inputs.bump }})
    runs-on: ubuntu-latest
    outputs:
      dir: ${{ steps.meta.outputs.dir }}
      slug: ${{ steps.meta.outputs.slug }}
      kind: ${{ steps.meta.outputs.kind }}
      version: ${{ steps.version.outputs.version }}
      tag: ${{ steps.version.outputs.tag }}
      dist_tag: ${{ steps.tag.outputs.dist_tag }}
      sha: ${{ steps.commit.outputs.sha || github.sha }}

    steps:
      - name: Harden the runner (audit egress)
        uses: step-security/harden-runner@fe104658747b27e96e4f7e80cd0a94068e53901d # v2.16.1
        with:
          egress-policy: audit

      - name: Checkout (full history)
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Resolve package metadata
        id: meta
        shell: bash
        env:
          INPUT_PACKAGE: ${{ inputs.package }}
        run: |
          set -euo pipefail
          case "$INPUT_PACKAGE" in
            simdeck)
              dir="."
              slug="simdeck"
              kind="npm-cli"
              ;;
            react-native-simdeck)
              dir="packages/react-native-inspector"
              slug="react-native-simdeck"
              kind="npm"
              ;;
            "@nativescript/simdeck-inspector")
              dir="packages/nativescript-inspector"
              slug="nativescript-simdeck-inspector"
              kind="npm"
              ;;
            simdeck-vscode)
              dir="packages/vscode-extension"
              slug="simdeck-vscode"
              kind="vsce"
              ;;
            *)
              echo "Unknown package input: $INPUT_PACKAGE" >&2
              exit 1
              ;;
          esac
          echo "dir=$dir"   >> "$GITHUB_OUTPUT"
          echo "slug=$slug" >> "$GITHUB_OUTPUT"
          echo "kind=$kind" >> "$GITHUB_OUTPUT"

      - name: Setup Rust toolchain (root simdeck only)
        if: ${{ steps.meta.outputs.kind == 'npm-cli' }}
        uses: dtolnay/rust-toolchain@stable

      - name: Configure git author
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

      - name: Bump version
        id: version
        shell: bash
        env:
          BUMP: ${{ inputs.bump }}
          PREID: ${{ inputs.preid }}
          PKG_DIR: ${{ steps.meta.outputs.dir }}
          SLUG: ${{ steps.meta.outputs.slug }}
        run: |
          set -euo pipefail
          cd "$PKG_DIR"

          if [[ "$BUMP" == "current" ]]; then
            version="$(node -p "require('./package.json').version")"
          elif [[ "$BUMP" == "prerelease" ]]; then
            new="$(npm version prerelease --preid "$PREID" --no-git-tag-version)"
            version="${new#v}"
          else
            new="$(npm version "$BUMP" --no-git-tag-version)"
            version="${new#v}"
          fi

          tag="${SLUG}-v${version}"

          echo "version=${version}" >> "$GITHUB_OUTPUT"
          echo "tag=${tag}"         >> "$GITHUB_OUTPUT"
          if [[ "$BUMP" == "current" ]]; then
            echo "Using current version ${version} (tag ${tag})"
          else
            echo "Bumped to ${version} (tag ${tag})"
          fi

      - name: Sync Rust CLI version
        if: ${{ steps.meta.outputs.kind == 'npm-cli' && inputs.bump != 'current' }}
        shell: bash
        env:
          NEW_VERSION: ${{ steps.version.outputs.version }}
        run: |
          set -euo pipefail
          perl -0pi -e 's/(\[package\]\s+name = "simdeck-server"\s+version = ")[^"]+(")/$1$ENV{NEW_VERSION}$2/s' packages/server/Cargo.toml
          cargo update --manifest-path packages/server/Cargo.toml -p simdeck-server --precise "$NEW_VERSION"

      - name: Resolve dist-tag
        id: tag
        shell: bash
        env:
          INPUT_TAG: ${{ inputs.dist-tag }}
          BUMP: ${{ inputs.bump }}
          PREID: ${{ inputs.preid }}
        run: |
          set -euo pipefail
          if [[ -n "$INPUT_TAG" ]]; then
            value="$INPUT_TAG"
          elif [[ "$BUMP" == "prerelease" ]]; then
            value="${PREID:-next}"
          else
            value="latest"
          fi
          echo "dist_tag=${value}" >> "$GITHUB_OUTPUT"

      - name: Commit and push version bump
        id: commit
        if: ${{ !inputs.dry-run }}
        shell: bash
        env:
          PKG_DIR: ${{ steps.meta.outputs.dir }}
          INPUT_PACKAGE: ${{ inputs.package }}
          NEW_VERSION: ${{ steps.version.outputs.version }}
          TAG: ${{ steps.version.outputs.tag }}
          BUMP: ${{ inputs.bump }}
        run: |
          set -euo pipefail
          if [[ "$BUMP" == "current" ]]; then
            git tag -f "${TAG}"
            git push --force origin "${TAG}"
            echo "sha=$(git rev-parse HEAD)" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          git add "${PKG_DIR}/package.json"
          if [[ -f "${PKG_DIR}/package-lock.json" ]]; then
            git add "${PKG_DIR}/package-lock.json"
          fi
          if [[ "${INPUT_PACKAGE}" == "simdeck" ]]; then
            git add packages/server/Cargo.toml packages/server/Cargo.lock
          fi
          git commit -m "chore(release): ${INPUT_PACKAGE}@${NEW_VERSION}"
          git tag "${TAG}"
          git push origin HEAD:${{ github.ref_name }}
          git push origin "${TAG}"
          echo "sha=$(git rev-parse HEAD)" >> "$GITHUB_OUTPUT"

      # A dry run never pushes, so the build jobs would otherwise check out the
      # pre-bump commit and build the previous version. Carry the bump across as
      # a patch so dry runs still exercise the real version end to end.
      - name: Capture version bump patch (dry-run)
        if: ${{ inputs.dry-run }}
        shell: bash
        run: |
          set -euo pipefail
          git diff > "${RUNNER_TEMP}/version-bump.patch"
          cat "${RUNNER_TEMP}/version-bump.patch"

      - name: Upload version bump patch (dry-run)
        if: ${{ inputs.dry-run }}
        uses: actions/upload-artifact@v4
        with:
          name: version-bump-patch
          path: ${{ runner.temp }}/version-bump.patch
          if-no-files-found: ignore

  # Windows is a supported target (see the integration-android matrix in ci.yml),
  # and package.json ships build/simdeck-bin-win32-x64.exe, but nothing built it
  # until now, so npm published a macOS-only tarball under `os: [darwin, linux,
  # win32]`. Build it here and hand it to the publish job.
  build-windows:
    name: Build Windows binary (win32-x64)
    needs: prepare
    if: ${{ needs.prepare.outputs.kind == 'npm-cli' }}
    runs-on: windows-latest

    steps:
      - name: Checkout release commit
        uses: actions/checkout@v4
        with:
          ref: ${{ needs.prepare.outputs.sha }}

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: package-lock.json

      - name: Setup Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Download version bump patch (dry-run)
        if: ${{ inputs.dry-run }}
        uses: actions/download-artifact@v4
        with:
          name: version-bump-patch
          path: ${{ runner.temp }}/bump

      - name: Apply version bump (dry-run)
        if: ${{ inputs.dry-run }}
        shell: bash
        run: |
          set -euo pipefail
          patch_file="${RUNNER_TEMP}/bump/version-bump.patch"
          if [[ -s "$patch_file" ]]; then
            git apply "$patch_file"
          fi

      - name: Install root dependencies
        run: npm ci --ignore-scripts

      - name: Build simdeck Windows binary
        run: npm run build:cli

      - name: Verify Windows artifact is a PE executable
        shell: bash
        run: |
          set -euo pipefail
          test -f build/simdeck-bin-win32-x64.exe
          header="$(head -c 2 build/simdeck-bin-win32-x64.exe)"
          if [[ "$header" != "MZ" ]]; then
            echo "build/simdeck-bin-win32-x64.exe is not a PE executable (header: ${header})" >&2
            exit 1
          fi
          ./build/simdeck-bin-win32-x64.exe --version

      - name: Upload Windows binary
        uses: actions/upload-artifact@v4
        with:
          name: simdeck-bin-win32-x64
          path: build/simdeck-bin-win32-x64.exe
          if-no-files-found: error

  release:
    name: Release ${{ inputs.package }} (${{ inputs.bump }})
    needs:
      - prepare
      - build-windows
    # build-windows is skipped for every package except the root simdeck CLI.
    if: ${{ !cancelled() && needs.prepare.result == 'success' && needs.build-windows.result != 'failure' }}
    runs-on: macos-latest
    environment:
      name: ${{ inputs.dry-run && 'npm-publish-dry-run' || 'npm-publish' }}

    steps:
      - name: Harden the runner (audit egress)
        uses: step-security/harden-runner@fe104658747b27e96e4f7e80cd0a94068e53901d # v2.16.1
        with:
          egress-policy: audit

      - name: Checkout release commit
        uses: actions/checkout@v4
        with:
          ref: ${{ needs.prepare.outputs.sha }}
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          registry-url: https://registry.npmjs.org
          cache: npm
          cache-dependency-path: |
            package-lock.json
            packages/client/package-lock.json
            packages/react-native-inspector/package-lock.json
            packages/nativescript-inspector/package-lock.json

      - name: Update npm (required for OIDC trusted publishing)
        run: |
          npm install -g npm@^11.5.1
          npm --version

      # A dry run never pushed the bump, so replay it here before installing.
      - name: Download version bump patch (dry-run)
        if: ${{ inputs.dry-run }}
        uses: actions/download-artifact@v4
        with:
          name: version-bump-patch
          path: ${{ runner.temp }}/bump

      - name: Apply version bump (dry-run)
        if: ${{ inputs.dry-run }}
        shell: bash
        run: |
          set -euo pipefail
          patch_file="${RUNNER_TEMP}/bump/version-bump.patch"
          if [[ -s "$patch_file" ]]; then
            git apply "$patch_file"
          fi

      - name: Setup Rust toolchain (root simdeck only)
        if: ${{ needs.prepare.outputs.kind == 'npm-cli' }}
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: aarch64-apple-darwin,x86_64-apple-darwin

      - name: Install native build dependencies
        if: ${{ needs.prepare.outputs.kind == 'npm-cli' }}
        run: brew install pkg-config x264

      - name: Install root dependencies
        run: npm ci

      - name: Install client dependencies (root simdeck only)
        if: ${{ needs.prepare.outputs.kind == 'npm-cli' }}
        run: npm ci --prefix packages/client

      - name: Install package dependencies
        if: ${{ needs.prepare.outputs.kind == 'npm' }}
        run: npm ci --prefix "${{ needs.prepare.outputs.dir }}"

      # ---------- Build (kind-specific) ----------

      - name: Build root simdeck (macOS arm64+x64 binaries + client + simdeck-test)
        if: ${{ needs.prepare.outputs.kind == 'npm-cli' }}
        run: |
          SIMDECK_BUILD_TARGET=aarch64-apple-darwin npm run build:cli
          cp build/simdeck-bin build/simdeck-bin-darwin-arm64

          SIMDECK_DISABLE_X264=1 SIMDECK_BUILD_TARGET=x86_64-apple-darwin npm run build:cli
          cp build/simdeck-bin build/simdeck-bin-darwin-x64

          lipo -create \
            -output build/simdeck-bin \
            build/simdeck-bin-darwin-arm64 \
            build/simdeck-bin-darwin-x64

          npm run build:client
          npm run build:simdeck-test

      - name: Verify CLI artifacts are macOS arm64+x64 binaries
        if: ${{ needs.prepare.outputs.kind == 'npm-cli' }}
        run: |
          test -x build/simdeck-bin
          test -x build/simdeck-bin-darwin-arm64
          test -x build/simdeck-bin-darwin-x64
          file build/simdeck-bin
          file build/simdeck-bin | grep -q 'arm64'
          file build/simdeck-bin | grep -q 'x86_64'
          file build/simdeck-bin-darwin-arm64 | grep -q 'arm64'
          file build/simdeck-bin-darwin-x64 | grep -q 'x86_64'

      # Downloaded after the macOS build so the build step cannot clobber it.
      - name: Download Windows binary
        if: ${{ needs.prepare.outputs.kind == 'npm-cli' }}
        uses: actions/download-artifact@v4
        with:
          name: simdeck-bin-win32-x64
          path: build

      - name: Verify Windows artifact is present
        if: ${{ needs.prepare.outputs.kind == 'npm-cli' }}
        run: |
          test -f build/simdeck-bin-win32-x64.exe
          file build/simdeck-bin-win32-x64.exe
          file build/simdeck-bin-win32-x64.exe | grep -q 'PE32+'

      # ---------- Apple codesign + notarize (root simdeck only) ----------

      - name: Detect Apple signing capability
        if: ${{ needs.prepare.outputs.kind == 'npm-cli' }}
        id: signing
        shell: bash
        env:
          HAS_CERT: ${{ secrets.APPLE_CERT_P12_BASE64 != '' }}
          HAS_NOTARY: ${{ secrets.APPLE_NOTARY_KEY_BASE64 != '' }}
          HAS_TEAM_ID: ${{ vars.APPLE_TEAM_ID != '' }}
          IS_DRY_RUN: ${{ inputs.dry-run }}
        run: |
          set -euo pipefail

          missing=()
          [[ "$HAS_CERT" == "true" ]] || missing+=("APPLE_CERT_P12_BASE64")
          [[ "$HAS_NOTARY" == "true" ]] || missing+=("APPLE_NOTARY_KEY_BASE64")
          [[ "$HAS_TEAM_ID" == "true" ]] || missing+=("APPLE_TEAM_ID")

          if [[ "${#missing[@]}" -eq 0 ]]; then
            echo "enabled=true" >> "$GITHUB_OUTPUT"
            echo "Apple signing + notarization enabled."
          elif [[ "$IS_DRY_RUN" == "true" ]]; then
            echo "enabled=false" >> "$GITHUB_OUTPUT"
            echo "::warning::Apple signing secrets/vars missing for dry-run: ${missing[*]}"
          else
            echo "enabled=false" >> "$GITHUB_OUTPUT"
            echo "::error::Apple signing secrets/vars missing: ${missing[*]}"
            echo "Refusing to publish an unsigned SimDeck native binary."
            exit 1
          fi

      - name: Setup signing keychain
        if: ${{ steps.signing.outputs.enabled == 'true' }}
        id: signing_keychain
        shell: bash
        env:
          APPLE_CERT_P12_BASE64: ${{ secrets.APPLE_CERT_P12_BASE64 }}
          APPLE_CERT_P12_PASSWORD: ${{ secrets.APPLE_CERT_P12_PASSWORD }}
          APPLE_TEAM_ID: ${{ vars.APPLE_TEAM_ID }}
        run: |
          set -euo pipefail

          KEYCHAIN_PATH="$RUNNER_TEMP/simdeck-signing.keychain-db"
          KEYCHAIN_PASSWORD="$(uuidgen)"
          CERT_PATH="$RUNNER_TEMP/cert.p12"

          security create-keychain -p "$KEYCHAIN_PASSWORD" "$KEYCHAIN_PATH"
          security set-keychain-settings -lut 21600 "$KEYCHAIN_PATH"
          security unlock-keychain -p "$KEYCHAIN_PASSWORD" "$KEYCHAIN_PATH"

          printf '%s' "$APPLE_CERT_P12_BASE64" | base64 --decode > "$CERT_PATH"
          security import "$CERT_PATH" \
            -k "$KEYCHAIN_PATH" \
            -P "$APPLE_CERT_P12_PASSWORD" \
            -T /usr/bin/codesign \
            -T /usr/bin/security

          security set-key-partition-list \
            -S apple-tool:,apple:,codesign: \
            -s -k "$KEYCHAIN_PASSWORD" "$KEYCHAIN_PATH" >/dev/null

          # Add temp keychain to user search list, preserving existing entries.
          EXISTING_KEYCHAINS="$(security list-keychains -d user | sed -e 's/^[[:space:]]*//' -e 's/"//g')"
          # shellcheck disable=SC2086
          security list-keychains -d user -s "$KEYCHAIN_PATH" $EXISTING_KEYCHAINS

          rm -f "$CERT_PATH"

          echo "Signing keychain ready at $KEYCHAIN_PATH"
          IDENTITY_REPORT="$(security find-identity -v -p codesigning "$KEYCHAIN_PATH")"
          echo "$IDENTITY_REPORT"

          SIGNING_IDENTITY="$(
            printf '%s\n' "$IDENTITY_REPORT" |
              awk -v team="(${APPLE_TEAM_ID})" '
                $0 ~ /"Developer ID Application:/ && index($0, team) {
                  print $2
                  exit
                }
              '
          )"

          if [[ -z "$SIGNING_IDENTITY" ]]; then
            echo "::error::No Developer ID Application identity for team ${APPLE_TEAM_ID} was found in the imported certificate." >&2
            exit 1
          fi

          echo "Using Developer ID Application identity for team ${APPLE_TEAM_ID}: ${SIGNING_IDENTITY}"
          echo "identity=$SIGNING_IDENTITY" >> "$GITHUB_OUTPUT"

      - name: Codesign simdeck binary
        if: ${{ steps.signing.outputs.enabled == 'true' }}
        shell: bash
        env:
          APPLE_SIGNING_IDENTITY: ${{ steps.signing_keychain.outputs.identity }}
        run: |
          set -euo pipefail

          KEYCHAIN_PATH="$RUNNER_TEMP/simdeck-signing.keychain-db"

          codesign --force \
            --options runtime \
            --timestamp \
            --keychain "$KEYCHAIN_PATH" \
            --sign "$APPLE_SIGNING_IDENTITY" \
            --entitlements scripts/release/simdeck.entitlements \
            build/simdeck-bin

          codesign --verify --strict --verbose=2 build/simdeck-bin
          echo "--- Signature details ---"
          codesign -dv --verbose=4 build/simdeck-bin

      - name: Notarize simdeck binary
        if: ${{ steps.signing.outputs.enabled == 'true' && !inputs.dry-run }}
        shell: bash
        env:
          APPLE_NOTARY_KEY_BASE64: ${{ secrets.APPLE_NOTARY_KEY_BASE64 }}
          APPLE_NOTARY_KEY_ID: ${{ secrets.APPLE_NOTARY_KEY_ID }}
          APPLE_NOTARY_KEY_ISSUER_ID: ${{ secrets.APPLE_NOTARY_KEY_ISSUER_ID }}
        run: |
          set -euo pipefail

          NOTARY_KEY_PATH="$RUNNER_TEMP/notary.p8"
          NOTARY_ZIP="$RUNNER_TEMP/simdeck-bin.zip"
          NOTARY_RESULT="$RUNNER_TEMP/notary-result.json"

          printf '%s' "$APPLE_NOTARY_KEY_BASE64" | base64 --decode > "$NOTARY_KEY_PATH"
          chmod 600 "$NOTARY_KEY_PATH"

          ditto -c -k --keepParent build/simdeck-bin "$NOTARY_ZIP"

          set +e
          xcrun notarytool submit "$NOTARY_ZIP" \
            --key "$NOTARY_KEY_PATH" \
            --key-id "$APPLE_NOTARY_KEY_ID" \
            --issuer "$APPLE_NOTARY_KEY_ISSUER_ID" \
            --wait \
            --timeout 30m \
            --output-format json > "$NOTARY_RESULT"
          NOTARY_STATUS=$?
          set -e

          echo "--- Notary submission result ---"
          cat "$NOTARY_RESULT" || true

          if [[ $NOTARY_STATUS -ne 0 ]]; then
            SUBMISSION_ID="$(node -e 'try{const j=require(process.argv[1]);process.stdout.write(j.id||"")}catch(e){}' "$NOTARY_RESULT" 2>/dev/null || true)"
            if [[ -n "${SUBMISSION_ID:-}" ]]; then
              echo "--- Notary log for ${SUBMISSION_ID} ---"
              xcrun notarytool log "$SUBMISSION_ID" \
                --key "$NOTARY_KEY_PATH" \
                --key-id "$APPLE_NOTARY_KEY_ID" \
                --issuer "$APPLE_NOTARY_KEY_ISSUER_ID" || true
            fi
            rm -f "$NOTARY_KEY_PATH" "$NOTARY_ZIP" "$NOTARY_RESULT"
            exit "$NOTARY_STATUS"
          fi

          rm -f "$NOTARY_KEY_PATH" "$NOTARY_ZIP" "$NOTARY_RESULT"

      - name: Cleanup signing keychain
        if: ${{ always() && steps.signing.outputs.enabled == 'true' }}
        shell: bash
        run: |
          KEYCHAIN_PATH="$RUNNER_TEMP/simdeck-signing.keychain-db"
          if [[ -f "$KEYCHAIN_PATH" ]]; then
            security delete-keychain "$KEYCHAIN_PATH" || true
          fi
          rm -f \
            "$RUNNER_TEMP/cert.p12" \
            "$RUNNER_TEMP/notary.p8" \
            "$RUNNER_TEMP/simdeck-bin.zip" \
            "$RUNNER_TEMP/notary-result.json" || true

      - name: Build npm package
        if: ${{ needs.prepare.outputs.kind == 'npm' }}
        run: npm --prefix "${{ needs.prepare.outputs.dir }}" run build

      - name: Package VS Code extension
        if: ${{ needs.prepare.outputs.kind == 'vsce' }}
        run: npm run package:vscode-extension

      # ---------- Publish (npm OIDC) ----------

      - name: Publish to npm (OIDC, dry-run)
        if: ${{ inputs.dry-run && (needs.prepare.outputs.kind == 'npm' || needs.prepare.outputs.kind == 'npm-cli') }}
        shell: bash
        env:
          NPM_CONFIG_PROVENANCE: "true"
          NODE_AUTH_TOKEN: ""
          DIST_TAG: ${{ needs.prepare.outputs.dist_tag }}
          PKG_DIR: ${{ needs.prepare.outputs.dir }}
        run: |
          set -euo pipefail
          unset NODE_AUTH_TOKEN
          rm -f ~/.npmrc || true
          if [[ -n "${NPM_CONFIG_USERCONFIG:-}" ]]; then
            rm -f "$NPM_CONFIG_USERCONFIG" || true
          fi
          cd "$PKG_DIR"
          npm publish --ignore-scripts --provenance --access public --tag "$DIST_TAG" --dry-run

      - name: Publish to npm (OIDC)
        if: ${{ !inputs.dry-run && (needs.prepare.outputs.kind == 'npm' || needs.prepare.outputs.kind == 'npm-cli') }}
        shell: bash
        env:
          NPM_CONFIG_PROVENANCE: "true"
          NODE_AUTH_TOKEN: ""
          DIST_TAG: ${{ needs.prepare.outputs.dist_tag }}
          PKG_DIR: ${{ needs.prepare.outputs.dir }}
        run: |
          set -euo pipefail
          unset NODE_AUTH_TOKEN
          rm -f ~/.npmrc || true
          if [[ -n "${NPM_CONFIG_USERCONFIG:-}" ]]; then
            rm -f "$NPM_CONFIG_USERCONFIG" || true
          fi
          cd "$PKG_DIR"
          npm publish --ignore-scripts --provenance --access public --tag "$DIST_TAG"

      # ---------- Publish (VS Code Marketplace) ----------

      - name: Publish VS Code extension (dry-run)
        if: ${{ inputs.dry-run && needs.prepare.outputs.kind == 'vsce' }}
        run: |
          ls -la build/vscode/
          echo "[dry-run] would: vsce publish --packagePath build/vscode/simdeck-vscode.vsix"

      - name: Publish VS Code extension
        if: ${{ !inputs.dry-run && needs.prepare.outputs.kind == 'vsce' }}
        env:
          VSCE_PAT: ${{ secrets.VSCE_PAT }}
        run: |
          if [[ -z "${VSCE_PAT:-}" ]]; then
            echo "VSCE_PAT secret is required to publish the VS Code extension." >&2
            exit 1
          fi
          npx --no-install vsce publish \
            --packagePath build/vscode/simdeck-vscode.vsix \
            --pat "$VSCE_PAT" \
            --skip-duplicate

      # ---------- GitHub Release ----------

      - name: Create GitHub release
        if: ${{ !inputs.dry-run }}
        env:
          GH_TOKEN: ${{ github.token }}
          TAG: ${{ needs.prepare.outputs.tag }}
          INPUT_PACKAGE: ${{ inputs.package }}
          NEW_VERSION: ${{ needs.prepare.outputs.version }}
          DIST_TAG: ${{ needs.prepare.outputs.dist_tag }}
          KIND: ${{ needs.prepare.outputs.kind }}
          BUMP: ${{ inputs.bump }}
        run: |
          set -euo pipefail
          notes="Released ${INPUT_PACKAGE}@${NEW_VERSION} (dist-tag: ${DIST_TAG})."

          assets=()
          case "$KIND" in
            npm-cli)
              assets+=("build/simdeck-bin")
              assets+=("build/simdeck-bin-win32-x64.exe")
              ;;
            vsce)
              assets+=("build/vscode/simdeck-vscode.vsix")
              ;;
          esac

          flags=()
          if [[ "$BUMP" == "prerelease" ]]; then
            flags+=(--prerelease)
          fi

          gh release create "$TAG" \
            ${assets[@]+"${assets[@]}"} \
            --title "$TAG" \
            --notes "$notes" \
            ${flags[@]+"${flags[@]}"}

      - name: Summary
        if: always()
        env:
          PKG: ${{ inputs.package }}
          BUMP: ${{ inputs.bump }}
          NEW: ${{ needs.prepare.outputs.version }}
          TAG: ${{ needs.prepare.outputs.tag }}
          DIST: ${{ needs.prepare.outputs.dist_tag }}
          DRY: ${{ inputs.dry-run }}
        run: |
          {
            echo "### SimDeck Release"
            echo ""
            echo "- Package: \`$PKG\`"
            echo "- Bump: \`$BUMP\`"
            echo "- Version: \`$NEW\`"
            echo "- Tag: \`$TAG\`"
            echo "- Dist-tag: \`$DIST\`"
            echo "- Dry-run: \`$DRY\`"
          } >> "$GITHUB_STEP_SUMMARY"
